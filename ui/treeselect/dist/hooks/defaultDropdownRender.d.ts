import React from 'react';
export declare const useDefaultDropdownRender: ({ fetchError, fetching, minSymbols }: {
    fetchError: string;
    fetching: boolean;
    minSymbols: number;
}) => (menu: React.ReactNode) => React.JSX.Element;
