import { IButtonRowProps, IFormButton } from '../../buttonsRow';
import React from 'react';
export declare const ButtonSimple: ({ id, button, context, componentProps, children, }: {
    id: string;
    button: IFormButton;
    context: unknown;
    componentProps: IButtonRowProps;
    children?: React.ReactNode;
}) => React.JSX.Element;
