import {Canvas, Controls, Meta, Source, } from '@storybook/blocks';

import * as inputControl from './inputControl.stories';

<Meta of={inputControl} />

# Checkbox

A checkbox is a square box that can be activated or deactivated when ticked.

Use checkboxes to select one or more options from a list of choices.

<Canvas of={inputControl.Secondary} source={{transform:(code)=>code.replace('component: () => {}', 'component: InputComponent')}} />
<Controls of={inputControl.Secondary} />
<Source of={inputControl.Secondary}  type="code" />
